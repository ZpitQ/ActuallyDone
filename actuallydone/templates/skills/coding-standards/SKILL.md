---
name: coding-standards
description: 本项目的代码风格、分层落点、命名前缀与禁用 API 清单。新建或修改源码文件时使用，讨论「这段代码该放哪一层/哪个目录/怎么命名」时也使用。
---

# 代码规范

<!-- 这是一份空模板：内容必须由你们自己填。
     通用的编码风格（缩进、命名规范）已经在语言的 linter 里了，写进技能是浪费上下文。
     值得写进来的只有三类：

     1. 分层落点 —— 新代码该放哪一层，判断依据是什么
     2. 命名约定 —— 只写那些不看约定就会写错的（前缀、字段单位、ID 类型）
     3. 禁用 API —— 曾经出过事故的写法，以及替代方案

     写法：每条给出「怎么写 / 为什么 / 反例」。没有「为什么」的规范会被绕过。 -->

## 分层落点

TODO：一张表说清每一层放什么、不放什么。例如：

| 目录 | 放什么 | 不放什么 |
| --- | --- | --- |
| `internal/api/` | HTTP 出入参解析与校验 | 业务判断 |
| `internal/service/` | 业务规则 | SQL |
| `internal/repo/` | 数据访问 | 业务规则 |

判断依据：TODO（例如「需要知道用户是谁的逻辑放 service，不需要的放 repo」）。

## 命名约定

TODO：只写不看约定就会写错的部分。典型的：

- 金额字段的单位与精度（分？元？小数几位？）
- ID 的类型与生成方式
- 表名/接口路径的前缀规则
- 时间字段的时区与存储形式

## 禁用 API 清单

TODO：每条写「禁什么 / 因为出过什么事 / 改用什么」。

```
### 禁止 <API 或写法>
出过的事：<一句话，最好带日期或 issue 号>
改用：<替代方案>
```

这一节是整份技能里价值最高的部分——它是事故的沉淀，通用规范里不会有。
数量控制在十条以内，超过就说明该拆成 `references/forbidden.md`。

## 与其他技能的关系

- 评审时按 [pr-review-checklist](../pr-review-checklist/SKILL.md) 对照本文件
- 写测试时的约定在 [test-driven-dev](../test-driven-dev/SKILL.md)
